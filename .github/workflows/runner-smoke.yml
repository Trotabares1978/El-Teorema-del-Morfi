name: Runner Smoke

on:
  push:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Runner probe
        run: |
          echo "RUNNER_OK"
          uname -a
          java -version
